# Tasks

- [x] Add OpenSpec requirement for explicit tokenized smoke readiness.
- [x] Implement `--include-token-smoke` parsing and plan wiring in `mvp:ready`.
- [x] Reuse smoke JSON parsing for the `token-smoke` readiness step.
- [x] Add focused tests for parsing, plan shape, success, failure, redaction,
  and role-mode rejection.
- [x] Update README readiness documentation.
- [x] Run security review for token/auth readiness diagnostic boundaries.
- [x] Run verification: focused ready tests, real tokenized ready command,
  `npm run check`, `npm test`, `npm run build`, and `npm run openspec:validate`.
