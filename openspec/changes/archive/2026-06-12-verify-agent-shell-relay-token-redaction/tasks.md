## 1. Coverage

- [x] 1.1 Add focused agent-shell integration coverage for successful connection to a token-protected relay.
- [x] 1.2 Verify local runtime logs and emitted runtime event records exclude the raw relay token while preserving normal relay-ready/join behavior.

## 2. Verification

- [x] 2.1 Run focused agent-shell runtime integration tests.
- [x] 2.2 Complete security review for the agent-shell token/log coverage diff.
- [x] 2.3 Run `npm run check`, `npm test`, `npm run build`, and `npm run openspec:validate`.
- [x] 2.4 Sync the completed OpenSpec delta into main specs and archive the change.
