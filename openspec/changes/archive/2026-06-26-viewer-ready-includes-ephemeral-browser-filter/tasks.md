## Tasks

- [x] Add the OpenSpec delta for viewer role-scoped ephemeral browser readiness.
- [x] Add the ephemeral browser role-filter step to `mvp:ready -- --role viewer`.
- [x] Add focused tests proving viewer role includes and validates the step while host/relay do not.
- [x] Run focused tests and strict OpenSpec validation.
- [x] Run full verification: `npm run check`, `npm test`, `npm run build`, `npm run openspec:validate`, `git diff --check`.
