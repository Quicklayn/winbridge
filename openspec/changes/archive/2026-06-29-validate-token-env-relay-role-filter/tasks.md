## 1. Implementation

- [x] 1.1 Add bounded token-env guidance to filtered command text output.
- [x] 1.2 Add default `mvp:ready` token-env relay role-filter validation.
- [x] 1.3 Add relay role-scoped token-env relay role-filter validation.

## 2. Tests

- [x] 2.1 Add command-kit tests for filtered relay token-env guidance without raw token output.
- [x] 2.2 Add ready-helper tests for default and relay-scoped token-env relay validation.
- [x] 2.3 Add drift tests for missing or raw token relay role-filter output.

## 3. Verification

- [x] 3.1 Run targeted command-kit and ready-helper tests.
- [x] 3.2 Run `npm run check`.
- [x] 3.3 Run `npm test`.
- [x] 3.4 Run `npm run build`.
- [x] 3.5 Run `npm run openspec:validate`.
