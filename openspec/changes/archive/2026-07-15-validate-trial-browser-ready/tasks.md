## 1. Ready Trial Validation

- [x] 1.1 Add browser to reviewed trial-plan role metadata in `mvp:ready`.
- [x] 1.2 Validate browser role-scoped trial output for viewer-scoped readiness.
- [x] 1.3 Keep runner roles unchanged and keep validation non-executing and bounded.

## 2. Tests And Docs

- [x] 2.1 Add focused `mvp-ready` tests for full browser trial coverage, viewer-scoped browser trial coverage, and drift failure.
- [x] 2.2 Update README readiness wording for browser trial-plan validation.

## 3. Verification

- [x] 3.1 Run focused `mvp-ready` tests.
- [x] 3.2 Run `npm run check`, `npm test`, `npm run build`, and `npm run openspec:validate`.
