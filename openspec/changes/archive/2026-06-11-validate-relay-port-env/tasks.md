## 1. Relay Port Config

- [x] 1.1 Add relay tests for valid default, explicit numeric, and ephemeral port configuration.
- [x] 1.2 Add relay tests rejecting malformed, partial, negative, fractional, and out-of-range port env values.
- [x] 1.3 Implement a strict `createRelayPortConfig` helper and use it in the relay entrypoint.
- [x] 1.4 Update docs if public relay configuration notes need clarification.

## 2. Verification

- [x] 2.1 Run focused relay tests.
- [x] 2.2 Run `npm run check`.
- [x] 2.3 Run `npm test`.
- [x] 2.4 Run `npm run build`.
- [x] 2.5 Run `npm run openspec:validate`.
- [x] 2.6 Archive the OpenSpec change after implementation and verification are complete.
