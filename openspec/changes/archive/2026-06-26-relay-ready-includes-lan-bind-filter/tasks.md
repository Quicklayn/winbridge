## Tasks

- [x] Add the OpenSpec delta for relay role-scoped LAN bind readiness.
- [x] Add the relay role-scoped LAN relay-only check and parser validation.
- [x] Add focused tests proving relay role includes and validates the LAN bind step while host/viewer do not.
- [x] Complete the security review for relay-command readiness impact.
- [x] Run focused tests and strict OpenSpec validation.
- [x] Run full verification: `npm run check`, `npm test`, `npm run build`, `npm run openspec:validate`, `git diff --check`.
