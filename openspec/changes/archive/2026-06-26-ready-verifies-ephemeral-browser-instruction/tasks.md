## Tasks

- [x] Add the OpenSpec delta for ephemeral command-plan readiness validation.
- [x] Add an `ephemeral-command-plan` step to the default `mvp:ready` plan.
- [x] Validate the ephemeral viewer/browser command contract without echoing command output.
- [x] Add focused ready-helper tests for pass, drift, and bounded diagnostics.
- [x] Run focused tests and strict OpenSpec validation.
- [x] Run full verification: `npm run check`, `npm test`, `npm run build`, `npm run openspec:validate`, `git diff --check`.
